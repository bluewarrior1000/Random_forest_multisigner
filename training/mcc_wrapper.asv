function mcc_wrapper(sarg)
    s = str2double(sarg);
    fprintf('Setting s to %02.0f and running...\n',s)
    fold = mod(s,5);
    tree_str = {'1_2','3_4','5_6','7_8'};
    
    fold = floor((s-1)/4) + 1;
    treenum = mod(s,4);
    
    treenum = s-(fold-1)*5;
    
    exit;
